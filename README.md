# deli01 — 出張リラクゼーション予約システム

派遣（出張）型の予約システム。核は「移動を挟んで次の予約が入るか」を判定する**空き枠算出エンジン**（[docs/spec.md](docs/spec.md) 5章）。

- 発注書 / 全仕様: **[docs/spec.md](docs/spec.md)**
- 開発ガイド・体制・規約: **[CLAUDE.md](CLAUDE.md)**

## 技術スタック

Next.js 15 App Router + TypeScript strict / PostgreSQL(Supabase) + PostGIS / Drizzle ORM / Supabase Auth / Tailwind CSS / date-fns-tz(`Asia/Tokyo`) / Zod / Vitest・Playwright / Vercel。

## ローカル起動

```bash
pnpm install
cp .env.example .env
pnpm db:up          # docker で Postgres+PostGIS 起動
pnpm db:migrate     # マイグレーション適用
pnpm db:seed        # シード投入
pnpm dev            # http://localhost:3000
```

## 環境変数

キー名は [.env.example](.env.example) 参照。ローカル開発とCIは `DATABASE_URL`（docker）だけで回る。Supabase・Google Maps・Anthropic・メールは発注者が用意する（完成確認まで未設定でも開発は進む）。

## デプロイ

`main` へのマージで Vercel 本番デプロイ。PR ごとにプレビュー。`main` へは PR 経由のみ（直接 push 禁止）。

---

## 判断ログ

仕様に明記が無く、自動進行の中で決めた事項と理由を時系列で残す（spec「判断に迷ったら」）。金銭・個人情報・法令に関わる判断はここに書かず、発注者に確認する。

| # | 日付 | フェーズ | 判断 | 理由 |
|---|---|---|---|---|
| 1 | 2026-08-29 | 0 | 開発DBはローカル docker(Postgres17+PostGIS) を既定にし、CIは GitHub Actions の postgres サービスコンテナで実行する | spec 権限ルール「開発は本番Supabaseを触らない」を満たしつつ、Supabase未提供でも CI 通過・統合テスト（実Postgres）まで自走するため。Supabase は preview/production DB と管理側Authに用いる |
| 2 | 2026-08-29 | 0 | 空リポジトリのブートストラップとして、統治ファイル（docs/spec.md・.claude/agents・CLAUDE.md・.gitignore・README・.env.example）で初回だけ `main` を直接作成する | 空リポジトリには PR のベースが無いため。以降の実装フェーズはすべて feat ブランチ + PR 経由に統一する |
| 3 | 2026-08-29 | 0 | パッケージマネージャは pnpm を採用 | 環境に pnpm 10 が導入済みで、モノレポ化・依存の再現性に優れるため |
| 4 | 2026-08-29 | 0 | Drizzle の運用を「型は `src/db/schema.ts` / マイグレーション適用は手書きSQL + 独自ランナー（`scripts/migrate.ts`）」に分離する。drizzle-kit は SQL 生成の補助に留め、適用の正は独自ランナー | spec 1-2「マイグレーションは SQL としてリポジトリに残す」を満たしつつ、PostGIS の `geography`・reservations の exclusion 制約（spec 4章）・RLS（spec 13-3）は Drizzle スキーマDSLで正確に表現できないため。適用済みは `schema_migrations` で冪等追跡し、advisory lock で並行適用の競合を防ぐ |
| 5 | 2026-08-29 | 0 | フェーズ0のシードは phase-0 スキーマ（用語辞書・サイト設定・フィールド定義）の初期値のみとし、spec 14章・18章の本体シード（エリア40・セラピスト25・ホテル30・顧客5,000・予約15,000）は該当テーブルが増える後続フェーズで段階投入する | フェーズ0にはまだ対象テーブルが無く投入不可能なため。空き枠エンジンの性能問題は密なデータでしか現れない（spec 14章）という警告を漏らさないよう、下記「シード段階投入の対応表」で追跡する |
| 6 | 2026-08-29 | 1 | 発注者指示により (a) 環境変数 `TZ`→`APP_TZ` にリネーム、(b) CMS内AI（spec 19章）のプロバイダを Anthropic→**OpenAI** に変更（`ANTHROPIC_API_KEY`→`OPENAI_API_KEY`）、(c) メール送信は v1 では顧客/オーナー向け自動通知（リマインド/週次レポート/キャンセル待ち通知・フェーズ20）にのみ使用 | 発注者の明示的な決定。spec 19章の本文（Anthropic 記載）は歴史的記録として残し、実装は OpenAI で行う。`APP_TZ` はアプリ層の変数名を OS 標準の `TZ`（docker/OS のタイムゾーン）と区別するため |
| 7 | 2026-08-29 | 1 | **Supabase 開発プロジェクトと Vercel を接続**。Supabase(PG17+PostGIS3.3.7, ref `zzkvjeqxqeayauoexqoi`) を preview/production DB とし、Vercel 本番 env に `DATABASE_URL`(6543 transaction pooler)/`NEXT_PUBLIC_SUPABASE_URL`/`APP_TZ` を設定。0000/0001 マイグレーション＋シードを Supabase にも適用。本番 https://deli01-zeta.vercel.app が health 200 で稼働 | 発注者が Supabase を用意し「デプロイまで捌いて」と指示。開発は引き続きローカル docker(5433)、Supabase は preview/production 用に分離。アプリ接続は serverless 向けに transaction pooler、マイグレーションは session pooler(5432)。停止条件② の Supabase/Vercel は解消済み（残: anon/service_role キーは Auth 実配線時に検証、OpenAI/Maps は該当フェーズで） |
| 8 | 2026-08-29 | 1 | 認証/権限の設計判断（architect）: (a) 枠外予約の許可ロールに reception を含める（常に理由必須）、(b) `audit_logs.id` は追記順が保たれる bigint identity、(c) 住所閲覧の可視終了側の上限は設けない | (a) spec 8-1 が電話受付を枠外予約の担い手として明記しているため。(b) 監査ログの時系列同定を確実にするため。(c) spec に終了上限の規定がなく、当日の道順再確認を妨げないため。RLS は接続ユーザーを1本にし GUC(`app.current_user_id`/`app.current_role`)＋`SET LOCAL ROLE app_runtime` で降格、未設定は fail-closed |
| 9 | 2026-08-29 | 2 | CMS 動的フォームの保存先として汎用の `entity_records`（entity+slug, draft/published jsonb）を1枚導入（spec 3-1 の therapist_profiles に倣う。EAV にしない）。RLS は owner/admin 全操作・reception は SELECT のみ・therapist 不可。認証は開発スタブ（owner）で、live Supabase 配線は後差し | spec 3-1「値は対象テーブルの jsonb カラムに入れる」に沿い、entity 横断で使い回せる保存先を1枚で用意。reception は電話受付で内容参照の可能性があり SELECT のみ許可。動的フォームは毎リクエストで `field_definitions` を読むため、項目追加が即フォームに反映される（受入条件を実証済み） |
| 10 | 2026-08-29 | 3 | フェーズ3で以下を先送りにする: (a) 画像アップロード UI（現状は data-URI プレースホルダ + メタ編集のみ / Storage 未配線）、(b) 画像の WebP 変換・リサイズ、(c) ページの履歴・巻き戻し（spec 3-2）、(d) 公開時の `revalidate`（ISR 再生成）、(e) ブロックの並べ替え・複製・追加 UI（現状は見出し編集とヒーロー画像添付のみ）。ヒーロー画像は draft ブロックの `imageId` 側に一本化し、ページ fields の `heroImageId` は保持のみ（誤消去防止）で編集経路にしない | spec 14章フェーズ3の完成条件（「画像を CMS から差し替えられる」）は data-URI プレースホルダ + ヒーロー画像添付 UI + プレビュー描画で実証できるため、アップロード実配線・履歴・並べ替えは後続フェーズに回してスコープを絞る。画像の一本化は fields 側 `heroImageId` を null 上書きしていた不具合の再発防止も兼ねる |
| 11 | 2026-08-29 | 4 | セラピスト公開のセキュリティ修正と先送りの明文化: (a) **セラピストの公開経路を掲載同意ゲート付きの専用公開（`publishTherapistProfile`）に一本化**。汎用 `publishEntityRecord` は `entity='therapist'` を先頭で拒否し、`DynamicForm` の汎用「公開する」ボタンも therapist 詳細画面では `showPublish={false}` で隠す（spec 3-7）。専用公開は同意チェック前に `buildZodSchema` で draft を fail-fast 検証する。(b) シードに therapist の `image_gallery` 型 `photo` フィールド（`is_public=true` / spec 2-2「写真（複数枚）」）を追加し、minato の draft.photo に未同意メディアを入れてゲート発火を実証。(c) 同意スキャンは論理削除フィールドも対象（`deleted_at is null` フィルタ除去）、退職処理の媒体特定は draft・published 両側を走査。**先送り: 公開プレビュー導線（`/therapists/[slug]`）はフェーズ5の公開ページ実装までの暫定リンク、変更履歴・巻き戻し（spec 3-2）は未実装、顔出し可否（`face_visibility`）のセラピスト単位設定と公開側反映はフェーズ5** | セキュリティ観点で「同意ゲートを唯一の公開経路にする」ことを最優先。汎用公開・埋め込みボタン・シードの3経路すべてでゲートを迂回できないよう塞ぐ。draft 検証を同意チェックより前に置き不完全な内容の公開を防ぐ。プレビュー導線・履歴・顔出し設定は公開ページ（フェーズ5）が未実装のため後続に回す |
| 12 | 2026-08-29 | 5 | フェーズ5の設計判断: (a) 公開側に loading.tsx / error.tsx を追加（ルートグループ・therapists・therapists/[slug] の3階層）。エラー表示は ↻ 記号 + aria-label="Retry" のみで日本語ゼロ。**エラー文言のCMS化（terminology/ui_labels 経由）は後続フェーズ**（現状は記号 + 英語 aria で暫定）。(b) face_visibility フィールドを公開側で反映: none=シルエット表示、eyes=目線帯（`globals.css` に `.eye-overlay::before` を定義）、face=通常表示。シードで aoi の写真を eyes にして実証。**プレースホルダSVG段階のため最小反映（値によって表示が変わる）に留め、実写真の本格的な顔加工（目線処理・マスク合成）はアップロードUI実装フェーズで対応**。(c) JSON-LD の XSS 対策として `JSON.stringify(personLd).replace(/</g, "\\u003c")` で `</script>` 脱出を防止。**サニタイズ方針の本対応（DOMPurify 等の導入・許可タグ定義）はフェーズ21**。(d) field-value.tsx の rich_text は `dangerouslySetInnerHTML` を廃止しタグ除去のプレーンテキスト化（公開側 XSS 防止・暫定）。(e) セラピストの `name` フィールド定義（sort_order=2、**is_public=true**）と氏名値（あおい/みなと/ひなた）をシードに追加し、**個人ページの主見出し**（buildTherapistView が name を view.name に抽出）と JSON-LD Person.name に `view.name \|\| view.catchCopy \|\| slug` の優先順位で使用（一覧カードは catch_copy 表示のまま。カードの氏名表示は後続）。**JSON-LD の `url` は site_settings に base URL キーが無いため相対 `/therapists/{slug}` のまま**（base URL が入り次第、絶対URL化）。(f) **最短案内/空き枠はフェーズ9まで placeholder + force-dynamic（EarliestSlot は time=null 固定）**。古い枠を出さないため（spec 2-3 / 2-7）、値が入るまでキャッシュに乗せない。(g) **CMS ブロック画像は `getPublicMediaMap(requireConsent:false)` を許容＝ページブロックに未同意の人物写真を貼れる隙が残る**。媒体に人物/素材の区分を持たせて塞ぐのはフェーズ6以降（推奨4）。(h) **モバイル崩れの E2E（Playwright）はフェーズ11へ集約**。 | 公開側の完了条件（spec フェーズ5 / 14章）: 3状態UI・顔出し可否反映・XSS対策の3点を実装。ローディング/エラーはNext.jsのConvention（loading.tsx/error.tsx）に乗りSuspense境界を自動設定。日本語ゼロ制約を保ちつつ記号とaria-labelで視覚的に十分なフィードバックを提供。暫定対応（エラー文言CMS化・顔加工・サニタイズ本対応・空き枠実値・媒体区分・E2E）は各後続フェーズへ明示的に先送り |
| 13 | 2026-08-29 | 5 | 非公開/未同意/退職セラピストは `notFound()` で not-found ページを描画する（プロフィールデータは一切出さない＝**データ露出なし**、統合テストで getPublicTherapist が null を返すことを検証済み）。ただし **Next.js 15.5 のストリーミング配下では notFound() が HTTP ステータス 200 を返す**（本来 404 が望ましい）。当面の緩和として generateMetadata で `robots: { index:false, follow:false }` を返し検索エンジンの索引付けを防ぐ。404 ステータス化（Next のバージョン挙動/レイアウトのストリーミング境界の調査）は後続の課題 | 完了条件（直書き日本語0・スマホ崩れない）と安全要件（非公開データを出さない）は満たしている。not-found ページの HTTP 200 は SEO 上の軽微な問題であり、noindex で実害を回避。データ保護は RLS 相当の公開クエリ（published/consent/is_hidden/status 絞り）+ 統合テストで担保 |
| 14 | 2026-08-29 | 5 | 公開のDB読取ページ（/therapists・/therapists/[slug]・/courses・/areas・/guide・/faq）を `revalidate=60`(ISR) から **`force-dynamic`（リクエスト時レンダ）** に変更 | Vercel 本番ビルドが ISR ページの**ビルド時プリレンダで Supabase(東京・無料枠プーラ)へ接続し 60秒タイムアウト**して失敗した（ローカルは高速な docker DB で通過）。force-dynamic ならビルド時にDBへ接続せず、実行時（health 200 が示す通り接続可能）に描画する。spec 2-7 の ISR 最適化は、DB のコロケーション or ビルド時DB非依存の実現後に再導入する（後続課題） |
| 15 | 2026-08-29 | 6 | 移動時間の設計判断（architect）: (a) **Google Maps Distance Matrix API 未キー**（停止条件②）のため車マトリクスは手動暫定値でシード。キー提供後に一括生成→CMS上書き。(b) areas はシード10件（spec 18章の40件は本体シード拡張時）。(c) **エリア管理UI（CRUD・マトリクスグリッド・要設定一覧）は未実装**＝完了条件外のため admin-ui 後続タスク。(d) 徒歩分は切り上げ（遅刻側に倒さない）・cap ちょうど(1600m)は徒歩圏・時間帯係数区間は [from,to)。(e) セラピスト個人の徒歩上限/車可否は chooseMode の引数受けにしてありフェーズ8でカラム追加のみ | 完了条件「徒歩と車が閾値で切り替わる」を純粋関数 chooseMode（≤1600=walk / 超かつ車可=car / 超かつ車不可=unreachable）で実装、境界値1599/1600/1601＋実PostGIS距離で検証。徒歩=PostGIS毎回計算・車=マトリクス+時間帯係数の分離（spec 5-1）。空き枠エンジン（フェーズ9）はこの純粋関数＋geo.ts を使う |
| 16 | 2026-08-29 | 7 | コース/オプション/ホテルの設計判断（architect）: (a) 管理UI（CRUD・料金表グリッド）は完了条件外のため後続 admin-ui。(b) `reservation_options`（価格/時間/バックのスナップショット）はフェーズ11で追加（後からオプション値変更で過去予約が変わらない方針を 0006 冒頭に明記）。(c) `therapist_courses`（対応可否・個人別指名料）はフェーズ8-9、`courses.nomination_fee_default` を既定に上書き。(d) reception の hotels insert/update（電話中仮登録）はフェーズ12。(e) 延長バックは仮55%（ランク別レートはフェーズ18）。Google Maps 未キーはフェーズ6 #15 のまま | 完了条件「ホテルの館内移動時間が加算される」を `arrivalBuffers`（destination=hotel のとき extra_minutes を到着に加算）で実装、unit+実DBで extra=12分ちょうど増える／住居・extra=0 では増えない／is_blocked は isHotelBookable=false を検証。金額は全て integer(円)+負数check、option_availability は行なし=全員対応 |
| 17 | 2026-08-29 | 8 | 出勤予定の設計判断（architect）: (a) shifts は**1セラピスト×1日1行**（unique(therapist_id, work_date)）。分割シフトが要るなら unique を外す判断を判断ログに残す。(b) **月カレンダー・繰り返しパターン（毎週火木/隔週の一括生成 / spec 3-3）は完了条件外のため後続 admin-ui タスク**。/admin/shifts は日別の最小編集（時間・待機場所・対応エリア・上限本数・当日欠勤ワンタップ）のみ。(c) 当日欠勤時の「既存予約一覧→振替促し」は予約テーブル導入後（フェーズ11以降）。連続施術の上限も予約導入時に設計。(d) /schedule は force-dynamic（毎リクエスト読取）で「60秒以内に反映」を満たす（フェーズ5 #14 のビルド時DB非接続と同方針。ISR 60秒への切替は #14 解消後）。(e) therapists に can_use_car / walk_cap_meters を追加（フェーズ6 #15(e) の予告どおり。chooseMode に引数で渡す）。(f) therapist ロールは自分の shift のみ select/update 可（当日欠勤ワンタップの RLS。本人用 UI はフェーズ14 マイページ）。(g) 公開出勤表は「出勤時間帯＋対応可能」表示に留め、確定枠は出さない（嘘の枠を出さない / spec 2-3。フェーズ9-10 で住所前提の再計算） | 完了条件「エリアで絞れる。60秒以内に反映」を最小スコープで満たす。エリア絞り込みは shift_areas の exists 1条件に集約し、出勤表とフェーズ9 の空き枠エンジン（spec 5-3 手順1-3）が同じテーブルを読む。シードは aoi=渋谷/恵比寿/目黒のみ・ren=全域・ren+2日目欠勤・minato=未公開で「出勤していても対応エリア外/未公開/欠勤なら出ない」を実データで再現（docs/shifts-schedule.md） |
| 18 | 2026-08-29 | 9 | 空き枠エンジンの設計判断（architect）: (a) 不等式の  は「案内開始」（施術は s+before から）。reservations 控えは start_at=s / end_at=serviceEndAt を想定。(b) **到着側の車の時間帯係数は「出発時刻」基準・固定点反復（最大4回）**で解く（spec は時刻基準未規定。遅刻側に倒れない決定的解釈）。(c) 15分グリッドは epoch 切り上げ（JST +9:00 が15分倍数なので壁時計一致）。(d) 最短案内(5-4)の概算はエリア代表点・既存予約/ホールド空・base座標未設定は0m扱い（要設定表示はフェーズ10）。(e) exclusion制約(depart_at〜free_at)・slot_holds スキーマはフェーズ11。engine 出力の depart_at/free_at がそのまま入る | 完了条件「第5-3章のテストが全部通る」を engine.test.ts の**12番号付きケース全て**＋ホテル/オプション/ホールド/検証で36テスト green。移動は既存純粋関数(chooseMode/walkMinutes/carMinutes/pickTimeModifier/travelBuffers/arrivalBuffers)を再利用し徒歩=距離ベース・車=マトリクス+係数を厳守。DB非依存の純粋関数でフェーズ11(予約)へ内訳をそのまま渡せる |
| 19 | 2026-08-29 | 10 | 公開空き枠の設計判断（public-ui）: (a) 空き枠は**キャッシュしない**（force-dynamic・毎回 engine 実行。セレクタ変更ごとに Server Action recomputeSlots）。(b) 個人ページの枠再計算は Server Action（area は UUID・ディープリンクを汚さない）／一覧のエリア絞りは `?area=<uuid>` の URL 駆動（リンク・SEO クロール可）。(c) dateISO 未指定時は最大7日先まで前方探索、明示時は単一日。明示エリアが対応外なら null=「嘘の枠を出さない」（spec 2-3）。(d) 最短案内の代表エリア概算は assumed=true で「〇〇区であれば案内可能」と条件明記（フェーズ9 R-4 解消）。(e) 住所での確定枠再計算はフェーズ11。(f) base 座標未設定=0m 扱いの「要設定」管理表示（estimated earliest slot が過度に楽観的になる警告）はフェーズ11 以降へ先送り（管理画面 CRUD と合わせて実装） | 完了条件「エリアとオプションを変えると枠が変わる」を統合テスト（渋谷 vs 目黒で枠セットが変わる・延長30分で L+30 され枠が減る）＋dev で実証。文言は ui_labels/用語辞書/DB 由来（公開側 直書き日本語0）。reservations/holds は空でフェーズ11 契約どおり |
| 20 | 2026-08-29 | 11 | 予約/仮押さえの設計判断（architect）: (a) 占有区間は depart_at〜free_at、ホールドは **reservations に status='held'** で insert し **exclusion 制約 no_therapist_overlap（btree_gist・tstzrange '[)'）** が裁定（slot_holds は session/expires 追跡に併設）。(b) 完全同時の GiST insert は投機エントリ待ちで deadlock(40P01) になりうる（裁定は正しい）→ createHold は 40P01/40001 を1回だけ再計算リトライし slot_taken/slot_gone に収束。(c) 楽観ロック=`where id=$ and version=$` の0行更新で version_conflict。(d) 生 Postgres エラーは列挙コード→CMS ラベル（booking_error_slot_taken=「他のお客様の予約が先に確定しました」）。(e) 住所ジオコーディング未配線→目的地はエリア代表点/ホテル所在地・addresses.location は null（配線後に「枠維持/代替提示」精緻化）。(f) 注文UIは1画面プログレッシブ開示の実用最小（誰でもいい/ポリシー同意/会員案内/ポイントは 12/15/16）。(g) funnel_events は追記専用（default privileges から明示 revoke） | 完了条件「同じ枠の同時取得で片方が弾かれ、離脱地点が計測される」を実 Postgres で実証（素の並行insert・createHold 並行2リクとも1成功1拒否・held は1件のみ、8連続 flake 0）。ファネルは hold/confirm を取引と同一TXで記録。金額は全て integer(円) |
| 21 | 2026-08-30 | 12 | 電話受付の設計判断（admin-ui）: (a) 受付の空き枠**表示**は公開と同一エンジン（getTherapistSlots）を再利用。ただし `createPhoneOrder` の予約時刻導出は簡易版（固定 25/10分バッファ）で、完全なエンジン導出ではない（占有区間の契約とは整合）。厳密なエンジン導出はフェーズ後半で精緻化。(b) 電話確認ゲート `canGenerateDispatch`（confirmed かつ phone_confirmed_at 非null のみ true／打診 canGenerateInquiry は confirmed で可）を純関数化＝フェーズ13の送信テンプレがこれを通す。(c) 電話注文は source='phone'・保存時に phone_confirmed_at 自動セット。Web は確認待ち。(d) 不成立は reason enum NOT NULL＋Zod で理由なし保存不可。(e) 枠外予約は can(override_slot)＋理由必須＋audit_logs。(f) CTI=22・リマインド=20・ポイント=16(現状0)・需要ヒートマップ=19 | 完了条件「リピーター自動補完・不成立記録・未確認は確定送信不可」を実DBで実証。受付は別ロジックにせず公開エンジンを再利用（spec 8-1）。管理側UIのため日本語は可・公開側(public)は日本語0を維持 |
| 22 | 2026-08-30 | 13 | 送信テンプレート・送信ログの設計判断: (a) **打診に「エリア」を載せるため非PIIの `{{エリア}}` 変数を追加**（spec 8-3 L785 の13変数に無いが、打診の必須内容「エリア・時間・コース・バック額」/ L787 を満たすため。`{{場所}}`=住所は打診禁止＝別物）。INQUIRY_FORBIDDEN_KEYS には含めない。(b) **打診用の個人情報非露出は純関数 `buildDispatchMessage` が構造的に保証**（inquiry のとき 場所/部屋番号/顧客名/電話番号/お好み を補間表から除去。CMS 本文に `{{電話番号}}` を書いても空文字化）。未定義変数は空文字で throw しない（受入 L1108/1109）。(c) **電話確認ゲートは Server Action の generate/record 双方で `canGenerateDispatch` を DB 生値から再検証**（UI 値を信用しない。受入 L1128）。(d) **reviewer 指摘(Blocker)対応: 確定用の既定テンプレから `{{電話番号}}` を除去**（spec 7-3 L709「顧客電話番号をセラピスト個人端末に残さない」／ 8-3 L788 の確定用列挙に電話番号無し。発信はマイページのアプリ内発信/転送番号=フェーズ14。変数自体は CMS に残し明示追加は owner 判断）。(e) **移動手段は `transport_fee>0 → 車` の逆写像**（fees.ts: walk=0/car>0・深夜加算は transport_fee に混入しない）。CMS で transportCar=0 にすると全件「徒歩」表示に化ける潜在バグ→予約側 travel mode スナップショット列はフェーズ14で検討。(f) **バック額はオプションバック合計のみの暫定**（コース単位バックレート payout_rates はフェーズ18。呼び出し側で差し替え、純関数は不変）。(g) `dispatch_logs` は**追記専用・監査対象**（bigint identity・update/delete を app_runtime から revoke＋ポリシー不在・therapist 不可・body_snapshot にコピー本文の控え / spec 8-3 L795）。テンプレ編集は audit_logs にも記録。(h) **先送り(Should): recordDispatch の body_snapshot は client 供給（「コピーした本文」を記録）**。ゲートは server 再検証済みで PII 露出は防げるが、監査本文の server 権威化（generate+log を単一TX）はフェーズ14 配車ボードで actions 再利用時に強化。`/admin/dispatch` は送信テキスト生成の最小画面で、フェーズ14 配車ボード（時系列ガント）が同 `src/lib/dispatch/actions.ts` を再利用する | 完了条件「打診用と確定用が出し分けられる」を純関数＋実DB統合で実証（打診にPII非露出・未確認は確定生成/記録不可・追記専用/RLSを実Postgres検証）。テスト 386→455（+69）。reviewer 合格（Blocker=確定既定の電話番号除去で解消）。管理側日本語可・公開側(public)は無変更で日本語0維持 |
| 23 | 2026-08-30 | 14 | 配車ボード/マイページの設計判断: (a) **addresses/customers の therapist RLS を精緻化**（フェーズ14必須条件 / spec 13-3・7-3）: 顧客住所は「担当予約 × status(confirmed/enroute/in_service/done) × **開始180分前ゲート** `now()>=start_at-interval '180 minutes'`」のみ therapist に可視。可視終了上限なし（判断ログ#8）。domain の `ADDRESS_VISIBLE_BEFORE_MIN=180` と SQL を一致。(b) **電話番号の列制御**（spec 7-3 L709）: 単一DBロール+GUC の RLS では列を隠せないため、customers への therapist 直接 select を default deny にし、**phone 列を持たない `customers_therapist_view`（security_barrier・同180分ゲート）** に一本化＋Server Action/型でも phone を返さない。**reviewer Blocker(B1) 対応: 単純ビューは自動更新可能＋0001 の default privileges で app_runtime に DML が付き、ビュー所有者(postgres)権限で base table に届き RLS をバイパスして therapist が customers.name を書き換えられた（実測）→ `revoke insert,update,delete on customers_therapist_view` で読取専用化**（`has_table_privilege` で UPDATE/INSERT/DELETE=false 確認）。(c) **顧客氏名も住所と同じ180分ゲートに入れた**（view の行スコープ統一・最小開示）。spec 明文は住所のみだが打診の氏名除去（8-3）と整合。3時間前から氏名を見せたい運用なら view の時刻条件を外すだけで緩和可。(d) **therapist 自己 status 更新**: RLS(行スコープ)＋BEFORE UPDATE トリガ（列 allow-list を to_jsonb 差分で担保・confirmed→enroute→in_service→done の隣接前進のみ・version+1・タップ列 set-once・違反は 42501）。純関数 `canTransition/nextStatus/isDelayed/isExitOverdue` は DB非依存で一次検証。(e) **到着/退出のワンタップ**は reservations に enroute_at/arrived_at/service_started_at/done_at を追加（exclusion/occupancy 契約は不変更）。**退出未記録アラート=`isExitOverdue`**（完了条件の主眼）。(f) **住所閲覧の監査**（spec 13-3）: `getTherapistTimelineCore` が返した住所に限り audit_logs(view/address) へ同一TXで追記。(g) **dev セラピストなりすまし**: `getTherapistDevSession(slug?)` は ADMIN_DEV_SESSION!=1 で必ず null（本番に impersonation 経路なし）。seed に therapist app_user（aoi/ren）追加。live Auth 配線は停止条件②。(h) reviewer S1 対応: `recordEmergency` を withUser 経由に（actor 詐称防止ポリシーを効かせる）。(i) **出退勤打刻(attendances/spec 3-5)は別テーブルの後続フェーズ**・打診返答/引き継ぎメモ(16)/稼ぎ・支払明細(18)/緊急連絡の実送信(20)はマイページで先送り | 完了条件「退出未記録がアラートになり、本人が自分の予定を見られる」を配車ボード(退出未記録/遅延の集約アラート・3ブロック描画・ステータス前進)＋マイページ(本人の当日予定・電話番号非表示)＋実DB統合で実証。テスト 455→614（+159）。reviewer 合格（Blocker B1=ビュー読取専用化で解消・S1=withUser化）。管理/本人向け日本語可・公開側(public)は無変更で日本語0維持 |
| 24 | 2026-08-30 | 15 | キャンセル・変更/当日延長/キャンセル待ちの設計判断（**サブエージェント月間上限到達のためリード本体で実装・自己レビュー・自己テスト**）: (a) **当日延長の可否＝完了条件**は既存の占有(depart_at〜free_at)+exclusion をそのまま使う。純関数 `canExtend`（延長後 free_at が後続 depart_at を超えるかで判定・半開'[)'で隣接可）で「押した瞬間の可否」を返し、間に合わなければ追加しない（受入 L1100）。**スキーマ変更不要**（既存列の更新）。(b) **管理者上書き**は override_slot+理由必須+audit だが、**物理的重複は exclusion が最終的に弾く**（override でも二重予約は作れない＝統合テストで実証）。overrideは「猶予/バッファを承知で詰める」意味で、後続を物理的に侵す延長は不可（後続の移動=別途リスケが要る=変更フロー）。(c) **therapist 本人の延長導線は0012のguardトリガ（列allow-list）と衝突**（end_at/free_at/total_amount は許可外）→ 延長の実updateは staff/サーバ経路（申請→受付が入れる）とし、本フェーズは配車ボードでなく `/admin/reservations`（staff）に延長・キャンセルUIを置く。本人申請フローの精緻化は live Auth 配線時。(d) **キャンセル**: reservations に cancelled_at/cancel_reason/cancel_kind(enum customer/shop/noshow) 追加。status→cancelled/noshow で exclusion の where から外れ**枠が空く**（=キャンセル待ちの契機）。キャンセル料は純関数 `cancellationFee`（CMS `site_settings.cancellation_policy` の段階制「何時間前から何%」・切り捨て・noshow/開始後は全額）で**算定まで**（売上台帳計上・バック配分は17/18）。既定雛形=24h前0%/3h前30%/当日50%（CMS変更が正）。(e) **キャンセル待ち** `waitlists`（希望条件=日付・時間帯・エリア・セラピスト・コース）は**枠を押さえない**（先着仮押さえ権なし / spec L660）。登録は特権 Server Action（クライアント直DB禁止）、通知はフェーズ20。(f) **先送り**: 顧客セルフの URL+電話番号でのキャンセル/変更ページ（spec 6 L647・公開側 ui_labels 整備要）と公開キャンセル待ちフォームは後続（本フェーズは staff 側で完了条件と機能を満たす） | 完了条件「後続に間に合わない延長が拒否される」を純関数＋**実Postgres統合で実証**（後続近接時に延長拒否・オプション非追加・占有不変／後続なしで成功し占有と金額が伸びる／override でも物理重複は exclusion で拒否／キャンセルで枠が空く／待ちは held を作らない）。テスト 614→640（+26）。管理側日本語可・公開側(public)無変更で日本語0維持 |
| 25 | 2026-08-30 | 16 | 顧客・指名・ポイント台帳・引き継ぎメモの設計判断（**サブエージェント上限が回復し委譲体制に復帰**。architect＋admin-ui×2＋qa＋reviewer）: (a) **追記専用ポイント台帳 `point_entries`**（bigint identity・update/delete を app_runtime から revoke＋RLS select/insert 分離・staff のみ・actor 詐称防止・0値/符号/lot 参照の check 群）。残高=sum(points)、`customers.cached_points` を after-insert トリガで加算（正は台帳）。**ロット=points>0 かつ lot_id is null**、use/expire/reverse は lot_id 必須で1ロット1行。同一顧客の並行 use は customers 行 `for update` で直列化。純関数 `consumeFifo`（FIFO・期限切れ除外・不足時 {ok:false}）/`expiring`/`earnedPoints`/`clampUse` を切り出し（17 revenue_lines・18 payout_lines が再利用）。**電話番号だけで残高照会・利用可**（完了条件 L1105）。会計連動（マイナス revenue_line・引当・バック基礎算入 / spec L844-849）は**フェーズ17へ明示先送り**（台帳増減で停止）。(b) **引き継ぎメモ `handover_notes`**: therapist select は「自分の担当(confirmed/enroute/in_service)×その顧客」= 次回担当のみ可視・done で消える・他セラピスト/本人は0行（受入 L1123）。insert は自分名義×実際に担当(in_service/done)した顧客のみ。**人格・容姿への言及禁止の注意書きをマイページ入力欄に常時表示**（spec L814）。マイページは therapist セッション注入の portal-actions 経由（RLS 適用）。(c) **指名NG `customer_therapist_ng`**: staff のみ閲覧（本人に「誰にNGされたか」を見せない）。**security definer＋search_path 固定の before-insert/update トリガ `reservations_ng_guard`** が公開Web・電話・管理の全経路で NG 組合せの予約を DB 層で拒否（superuser 直 insert でも check_violation / spec L808）。指名料は `therapist_courses`（is_available・nomination_fee null=コース既定・予約時参照切替は18）。(d) 付与率5%/失効365日/利用上限下限は雛形＝CMS `site_settings.point_policy` 変更が正（金銭・cancellation_policy と同じ整理）。earn は done のみ・1予約1回。**reviewer 指摘対応**: **B1**=/admin/points と listNgPairs の `therapists.name`（存在しない列）を entity_records 由来へ修正＝NG 登録フォームが空になる不具合解消。**B2**=done 付与の二重付与 TOCTOU を**部分 unique index `point_entries_reservation_earn_uniq`（reservation_id where type='earn' and reason='reservation_done'）**で DB 担保＋23505 を握って「付与済み」返却。**S4**=指名NG actions を withUser 降格経由に（staff RLS を実際に効かせる）。**S5**=point_policy の expiry_days 未設定は無期限でなく雛形365日へ倒す（金銭・引当が無限に積まれない）。**S2**=cron(20) 前に失効が走らない問題へ /admin/points に「失効バッチ手動実行」ボタン。**S3**=電話注文のポイント利用を成功後に締めて二重記録防止。**フェーズ17送り（判断ログ）**: S1 ロット参照整合の before-insert トリガ（staff 手 insert 対策・アプリ層では担保済み）・N4 逆仕訳のロット戻し規約・N2 handover insert の直近予約絞り・N1 NG エラーの受付向け文言変換・S2 の利用可能P併記。 | 完了条件「ポイントが電話注文でも動き、次回予約でメモが担当者に出る」を純関数＋**実Postgres統合**で実証（電話番号で earn/残高/利用・FIFO・失効・追記専用の update/delete 拒否・次回担当のみメモ可視・他者0行・NG 全経路ブロック）。テスト 640→813（+173）。reviewer B1/B2 解消で合格。管理/本人向け日本語可・公開側(public)無変更で日本語0維持 |
| 26 | 2026-08-30 | 17 | 売上台帳・会計・回数券・経費の設計判断（architect＋admin-ui＋qa＋reviewer）: (a) **追記専用の売上台帳 `revenue_lines`**（独立行計上・合算しない / spec L856。update/delete revoke＋RLS＋符号 check＋`reversal_of` 逆仕訳追記＝上書きしない）。集計は revenue_lines だけを読む（spec L858）。**二重計上防止は部分 unique**（core=course/ticket_redeem・singleton=nomination/transport/midnight/point_use・option・**reversal_of=二重逆仕訳防止[reviewer S4]**）。`payments`（支払内訳・併用可 / spec L855。**ポイントは支払でなく point_use 値引で表現＝'point' を入力 Zod から除外[reviewer S6]**）。`ticket_entries`（回数券=前受金・追記専用・ロット追跡）。`expenses`（経費・spec L868）。純関数 `src/domain/accounting`（revenueBreakdown/ticketAllocation/deferredRevenue/pointLiability/settlement）。**完了条件「前受金・ポイント引当・売上・経費が分けて出る」= `getAccountingSummary` が4区分を別枠返却**。(b) **ポイント会計連動（フェーズ16 先送り分を結線 / spec L844-849）**: 利用→`point_use` のマイナス revenue_line（1本）・付与→引当（pointLiability・売上に立てない）・失効→戻入。運用順序は done→usePoints→postReservationRevenue。**reviewer Blocker(B1) 対応: 計上後にポイント利用すると値引行を立てられず売上が過大固定される欠落経路→ `spendPointsCore` に「予約が計上済みならポイント利用を拒否」ガードを追加**（同一TX・race安全）。**reviewer S1**: 全額回数券振替済み予約の post で payments 重複挿入を防ぐ（fee 行が立つ初回のみ payments 記録）。(c) **回数券は発注者判断で UI 当面非提供**（`/admin/accounting` の発行UI除去）。**台帳・アクション・テストはバックエンドに温存**（前受金→振替→逆仕訳・端数配分は実装/テスト済み。再有効化は容易）。前受金カードは残置（回数券残の器・未発行なら¥0）。(d) **金銭判断（spec 明文を超える解釈・発注者確認事項）**: ①売上認識=**done のみ** ②計上日=**start_at 基準** ③回数券売上=**券面配分額**（定価でない・差額は発行時割引で売上に出さない）④前受金=**発行時に全額**負債計上 ⑤course 行=**残差**（reservations にコース価格スナップショット列が無いため。合計=total は不変。**深夜設定を後から変えると過去予約の course/midnight 配分が乖離**→ midnight スナップショット列追加を **フェーズ18 着手前に解消推奨[reviewer S2]**）⑥ポイント利用=`point_use` 行種（'discount' は直前割用に温存）⑦**完全逆仕訳後の同一予約再計上は不可**（unique 設計）。**フェーズ18 送り[reviewer S3/S5]**: 一般 revenue_line の汎用逆仕訳 action（誤計上の修正経路）・支払方法内訳の入力UI。 | 完了条件を純関数＋**実Postgres統合**で実証（独立行計上・point_use のマイナス行分離・付与は引当のみ・回数券の前受金→振替→逆仕訳・経費別枠・getAccountingSummary の4区分分離・追記専用の update/delete 42501・並走二重計上 unique 拒否）。テスト 813→931（+118）。reviewer Blocker(B1) 解消・S1/S4/S6 同PR修正。**金銭7判断は発注者確認待ち**。管理側日本語可・公開側(public)無変更で日本語0維持 |
| 27 | 2026-08-30 | 18 | 報酬（業務委託バック）の設計判断（**fable 控えめ運用へ移行**＝architect を核心1回のみ・UI は sonnet・**reviewer は使わずリード自己レビュー＋qa の15章テスト**で担保）: (a) **`payout_rates`（適用開始日つき・個別>ランク別>既定）**＋**追記専用台帳 `payout_lines`（calc_note jsonb に レートID/元金額/計算式/適用日をスナップショット / spec L913・受入 L1098）**＋**`payouts` 締め（closed でロック・security definer トリガ P0018/P0019 が期間内 insert と closed 後変更を DB 層で拒否・修正は逆仕訳のみ / 受入 L1097）**＋`payout_deductions`（源泉徴収 kind 含む）。**セラピスト単位の締め期間 daterange exclusion** で重複拒否。純関数 `resolveRate`（その日有効なレートを優先順位で決定的に選ぶ / 受入 L1096）・`computePayoutLine`（fixed円/rate=floor(base×%/100)・calc_note 生成）・`buildReservationPayout`・`settlePayoutPeriod`。**率は整数%厳守**（spec の numeric は不採用）。(b) **完了条件「15章の報酬テストが全部通る」を実証**: L1094 レート改定後も closed 期間の過去報酬不変（改定=旧行 effective_to 打切り＋新規行・value 上書き無し）／L1095 回数券消化でもバック発生（金額計算は現金有無で分岐しない）／L1096 個別>ランク別>既定／L1097 締め後ロック・修正は逆仕訳・二重逆仕訳 unique 拒否／L1098 calc_note に根拠／L1134 therapist は自分の payout_lines のみ select（他人は0行・RLS）・追記専用 update/delete 42501。(c) **セラピスト向け即時集計** `getMyEarnings`（今日の稼ぎ/今月見込み/確定額/category別内訳/支払履歴・spec 11-5・therapist セッション経由）をマイページに。**レートグリッド管理 `/admin/payouts`**（個別特例に印 / spec 18-5）＋**未計上 done 予約→報酬計上**（advanceReservationStatus は改変せず手動計上に統一＝「済み＝意図的に押した」監査性重視）＋締め・支払記録。**全銀CSV はフェーズ19 送り**。(d) **金銭の既定（spec 明文を超える解釈・発注者確認事項）**: 源泉徴収=**既定オフ**（額自動判定せず控除手入力・税理士確認要 / spec L936）・インボイス登録番号は締め時スナップショット・バック基礎=**値引前**（CMS 切替可 / spec L920）・noshow=**交通費のみ**（spec L919）・キャンセル料バック率=レート設定制・回数券/ポイントは基礎に**含める**（spec L848/L917）。(e) seed に therapist_ranks 割当（あおい=プレミア/れん=レギュラー/他=新人）＋ランク別レート雛形（新人50/レギュラー55/プレミア65%・指名交通100%）＋個別特例2（spec 18-4/18-5）。**リード修正**: 統合時に seed のランク UUID 衝突（migration 0016 が固定UUIDで既投入→名前解決に修正）・payout_rate seed の非hex UUID(`r1r1r1r1`→`a1a1a1a1`)・**既存の日跨ぎフレーク**（フェーズ14 タイムラインテストが now+offset の JST 日付でなく "today" で問い合わせ→18時以降落ちる→予約 start_at の JST 日付で問い合わせに修正）・**payout 統合テストを seed デモレートから隔離**（共有ランクでなくテスト専用ランクを生成）を自己対応。 | 完了条件「15章の報酬テストが全部通る」を純関数（23）＋実Postgres統合（18）で実証。テスト 931→972（+41・フレーク恒久修正含む）。fable は architect 1回のみ・reviewer 不使用（自己レビュー＋grep＋15章テストで金銭台帳の追記専用/RLS/締めロック/calc_note を担保）。管理/本人向け日本語可・公開側(public)無変更で日本語0維持 |

### 本番公開前チェックリスト（判断ログ #10 の解消条件）

フェーズ3で暫定対応した項目を本番前に必ず解消する:

- [ ] **ダミー画像（data-URI SVG）を本物の写真・イラストに差し替える**（`is_placeholder=true` の media 全件 / spec 3-7。実在人物の顔出しは同意フラグと顔出し可否を確認）
- [ ] 画像アップロード UI と Storage（Supabase Storage 等）を配線し、WebP 変換・リサイズを通す
- [ ] `ADMIN_DEV_SESSION` を本番で設定しない（live Supabase Auth へ差し替え）

### シード段階投入の対応表（判断ログ #5 の追跡）

spec 18章の本体シードを、対象テーブルが揃うフェーズで投入する。各フェーズの完了時にこの表を更新する。

| データ | 目標規模（spec 18章） | 投入フェーズ | 状態 |
|---|---|---|---|
| 用語辞書・サイト設定・フィールド定義 | 初期セット | 0 | ✅ 済 |
| コース / オプション / 指名・加算・交通費 | spec 18-1〜18-3 | 7 | 予定 |
| エリア40 / 移動時間マトリクス | 40エリア | 6 | 予定 |
| ホテル30 | 30施設 | 7 | 予定 |
| セラピスト25（ランク3段階・個別特例3人 / spec 18-5） | 25名 | 4 | 予定 |
| 出勤予定 | 現実的分布 | 8 | 🟡 済（直近5日分の最小デモ: エリア限定/全域/当日欠勤/未公開の4類型。25名×現実的分布は本体シード拡張時） |
| 顧客5,000（4割ポイント保有） | 5,000 | 16 | 予定 |
| 予約1年分15,000（3割オプション・土日夜偏り・指名率6割・キャンセル1割） | 15,000 | 11以降 | 予定 |
| 報酬レート・ポイント初期設定 | spec 18-4・18-6 | 18 / 16 | 予定 |
| ダミー画像（ヒーロー2・コース4・セラピスト25 / placeholder タグ） | 31点 | 3〜4 | 🟡 済（暫定 data-URI SVG。ヒーロー2・コース2・セラピスト1 を投入。本番前に本物へ差し替え） |

## 停止条件で発注者に依頼する事項（判明分）

以下は spec 停止条件②（発注者にしか用意できないもの）。未提供でも開発とCIは進むが、該当フェーズの「プレビュー/本番」到達に必要:

- **Supabase 開発プロジェクト**（URL / anon key / service role key）— 管理側 Auth、preview/production DB
- **Vercel プロジェクト**（GitHub 連携）— プレビュー URL（フェーズ0の完了条件「プレビューが出る」）
- **Google Maps Distance Matrix API キー** — 車のエリア間移動時間の初期一括生成（フェーズ6/9。未提供時は直線距離×係数の暫定値で代替）
- **Anthropic API キー** — CMS内AIアシスタント（フェーズ21）
- **メール送信プロバイダ** — リマインド・週次レポート（フェーズ20）
